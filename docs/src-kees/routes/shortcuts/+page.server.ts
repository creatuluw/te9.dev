import type { PageServerLoad } from './$types';

/**
 * Server-side load function for shortcuts page
 */
export const load: PageServerLoad = async ({ locals }) => {
	// Return empty data for now - placeholder page
	return {};
};





































